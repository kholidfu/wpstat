# WP Auto Login with Selenium

## What?
Tool ini berguna untuk login ke WP secara otomatis menggunakan selenium.

## How?
Rename `credentials.json.ex` -> `credentials.json`, kemudian isikan:
- url
- username
- password
  
Jalankan:

```python main.py```